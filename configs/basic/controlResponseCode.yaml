controlResponseCode:
  # Settings that will apply to all codes if they are not overridden in the code itself
  _general:
    #  Priority, says what will be executed if there is a valid response status
    priority: view

    # The name of the template engine with which the template will be loaded
    engine: big

  # Response code
  404:
    view: notFound